package com.example.currency;

import java.io.Serializable;

/**
 * The base class of all Models, which take charge of data handling in activities.
 * Each activity can hold own's model, all can share model with other activities.
 */
public abstract class BaseModel extends Object implements Serializable
{
}
